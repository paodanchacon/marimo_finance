# Copiar a .env (gitignored) y ajustar si hace falta.
# Estos valores alimentan tanto docker-compose.yml como src/db.py.

DB_ROOT_PASSWORD=root_dev_password
DB_NAME=marimo_finance
DB_USER=marimo
DB_PASSWORD=marimo_dev_password
DB_HOST=localhost
DB_PORT=3306
